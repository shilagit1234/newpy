from django.apps import AppConfig


class CConfig(AppConfig):
    name = 'c'
from django.apps import AppConfig

from django.apps import AppConfig


class CConfig(AppConfig):
    name = 'c'

class CConfig(AppConfig):
    name = 'c'
